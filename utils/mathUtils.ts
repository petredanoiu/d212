/**
 * Rounds a number to the nearest integer following mathematical rounding rules
 * @param num - The number to round
 * @returns The rounded integer
 */
export const roundToInteger = (num: number): number => {
    return Math.round(num);
};